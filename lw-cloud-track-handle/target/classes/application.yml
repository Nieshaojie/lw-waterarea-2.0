server:
  port: 9981

spring:
  profiles:
    active: dev
  application:
    name: lw-cloud-track-handle

mybatis:
  type-aliases-package: com.mskyeye.handler.model
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true